var started = Sys.time()
var count = 0
for i in range(0, 1000000):
    if "hello world" == "hello world":
        count = count + 1
    end
end
var ended = Sys.time()
println(count)
println("TIME_PF")
println(ended - started)
