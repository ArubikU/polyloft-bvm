var started = Sys.time()
var s = 0
for i in range(0, 1000):
    var inner = {}
    inner["val"] = i
    s = s + inner["val"]
end
var ended = Sys.time()
println(s)
println("TIME_PF")
println(ended - started)
