var started = Sys.time()
var s = 0
for i in range(0, 5000000):
    if i % 2 == 0:
        s = s + 1
    elif i % 3 == 0:
        s = s + 2
    else:
        s = s - 1
    end
end
var ended = Sys.time()
println(s)
println("TIME_PF")
println(ended - started)
