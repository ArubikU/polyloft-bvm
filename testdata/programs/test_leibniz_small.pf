def leibniz_pi(n: int) -> number:
    let s = 0.0
    let sign = 1.0
    for i in range(0, n):
        s += sign / (2.0 * i + 1.0)
        sign = sign * -1.0
    end
    return s * 4.0
end
println(leibniz_pi(10))
